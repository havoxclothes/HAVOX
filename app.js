let PRODUCTS=[];let STORE={delivery:350,payment:'COD and bank payment',contact:'075 180 4730',deliveryInfo:'Islandwide delivery across Sri Lanka. Delivery fee is Rs. 350. Estimated delivery time is 2–5 working days.'};let SIZE_GUIDE={};let SOCIAL={};let cart=JSON.parse(localStorage.getItem('havoxCart')||'[]');const $=s=>document.querySelector(s);const money=n=>`Rs. ${Number(n).toLocaleString()}`;
function parseData(text){const get=k=>{const m=text.match(new RegExp('^'+k.replace(/[.*+?^${}()|[\\]\\\\]/g,'\\$&')+'[ \\t]*:[ \\t]*(.*)$','mi'));return m?m[1].trim():''};const products=[{id:1,name:'straw hat will',color:'black',gsm:190,price:2100,stock:{M:2,L:4,XL:2},image:'1. Straw hat will.png'},{id:2,name:'cursed king',color:'black',gsm:190,price:2100,stock:{M:1,L:2,XL:1},image:'2.Cursed king.png'},{id:3,name:'sun breather',color:'white',gsm:220,price:2300,stock:{M:2,L:4,XL:2},image:'3.Sun breather.png'},{id:4,name:'Destiny forged',color:'white',gsm:220,price:2300,stock:{M:1,L:2,XL:1},image:'4. Destiny forged.png'}];STORE.delivery=Number(get('DELIVERY_FEE_Rs'))||350;STORE.payment=get('PAYMENT_METHODS')||STORE.payment;STORE.contact=get('CONTACT_INFO')||STORE.contact;STORE.deliveryInfo=get('DELIVERY_INFO')||STORE.deliveryInfo;SIZE_GUIDE={M:{chest:get('M_CHEST'),length:get('M_LENGTH'),shoulder:get('M_SHOULDER')},L:{chest:get('L_CHEST'),length:get('L_LENGTH'),shoulder:get('L_SHOULDER')},XL:{chest:get('XL_CHEST'),length:get('XL_LENGTH'),shoulder:get('XL_SHOULDER')}};SOCIAL={Instagram:get('INSTAGRAM'),TikTok:get('TIKTOK'),Facebook:get('FACEBOOK'),YouTube:get('YOUTUBE'),WhatsApp:get('WHATSAPP')};return products}
function renderSizeGuide(){$('#sizeGuide').innerHTML=['M','L','XL'].map(s=>`<tr><td>${s}</td><td>${SIZE_GUIDE[s].chest||'—'}</td><td>${SIZE_GUIDE[s].length||'—'}</td><td>${SIZE_GUIDE[s].shoulder||'—'}</td></tr>`).join('')}
function renderSocial(){const links=Object.entries(SOCIAL).filter(([,url])=>url).map(([name,url])=>{let href=url;if(name==='WhatsApp'){const digits=url.replace(/\D/g,'');href=digits.startsWith('94')?`https://wa.me/${digits}`:`https://wa.me/94${digits.replace(/^0/,'')}`}return `<a href="${href}" target="_blank" rel="noopener noreferrer">${name.toUpperCase()} ↗</a>`}).join('');$('#socialLinks').innerHTML=links||'<span>Social links coming soon.</span>'}
function renderProducts(){$('#products').innerHTML=PRODUCTS.map(p=>`<article class="product"><div class="product-image"><img src="${encodeURI(p.image)}" alt="${p.name}"></div><div class="product-info"><div class="product-top"><h3>${p.name}</h3><span class="price">${money(p.price)}</span></div><p class="desc">Premium HAVOX streetwear. Details will be expanded as the collection grows.</p><div class="meta"><span>${p.color.toUpperCase()}</span><span>• ${p.gsm} GSM</span></div><div class="sizes" data-product="${p.id}">${Object.entries(p.stock).map(([s,q])=>`<button data-size="${s}" ${q<1?'disabled':''}>${s}</button>`).join('')}</div><p class="stock" id="stock-${p.id}">SELECT A SIZE</p><button class="add" data-add="${p.id}">ADD TO BAG</button></div></article>`).join('');document.querySelectorAll('.sizes').forEach(group=>group.addEventListener('click',e=>{if(!e.target.matches('button'))return;group.querySelectorAll('button').forEach(b=>b.classList.remove('selected'));e.target.classList.add('selected');const p=PRODUCTS.find(x=>x.id==group.dataset.product);$('#stock-'+p.id).textContent=`${p.stock[e.target.dataset.size]} IN STOCK`}));document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>{const p=PRODUCTS.find(x=>x.id==b.dataset.add);const selected=document.querySelector(`.sizes[data-product="${p.id}"] .selected`);if(!selected){alert('Please select a size first.');return}addToCart(p,selected.dataset.size)})}
function addToCart(p,size){const existing=cart.find(x=>x.id===p.id&&x.size===size);const used=cart.filter(x=>x.id===p.id&&x.size===size).reduce((n,x)=>n+x.qty,0);if(used>=p.stock[size]){alert('That size is out of stock.');return}if(existing)existing.qty++;else cart.push({id:p.id,size,qty:1});save();openCart()}
function save(){localStorage.setItem('havoxCart',JSON.stringify(cart));renderCart()}
function renderCart(){const box=$('#cartItems');if(!cart.length)box.innerHTML='<p style="color:#777">Your bag is empty. Pick a design and size to begin.</p>';else box.innerHTML=cart.map((x,i)=>{const p=PRODUCTS.find(p=>p.id===x.id);return `<div class="cart-item"><img src="${encodeURI(p.image)}" alt=""><div><h4>${p.name}</h4><p>${x.size} · ${money(p.price)}</p><div class="qty"><button data-dec="${i}">−</button><b>${x.qty}</b><button data-inc="${i}">+</button></div></div><button class="remove" data-remove="${i}">REMOVE</button></div>`}).join('');const subtotal=cart.reduce((n,x)=>n+PRODUCTS.find(p=>p.id===x.id).price*x.qty,0);$('#subtotal').textContent=money(subtotal);$('#delivery').textContent=cart.length?money(STORE.delivery):money(0);$('#total').textContent=money(subtotal+(cart.length?STORE.delivery:0));$('#bagCount').textContent=cart.reduce((n,x)=>n+x.qty,0);document.querySelectorAll('[data-inc]').forEach(b=>b.onclick=()=>changeQty(+b.dataset.inc,1));document.querySelectorAll('[data-dec]').forEach(b=>b.onclick=()=>changeQty(+b.dataset.inc,-1));document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{cart.splice(+b.dataset.remove,1);save()})}
function changeQty(i,d){const x=cart[i],p=PRODUCTS.find(p=>p.id===x.id);if(d>0&&x.qty>=p.stock[x.size]){alert('No more stock available for this size.');return}x.qty+=d;if(x.qty<1)cart.splice(i,1);save()}
function openCart(){$('#cart').classList.add('open');$('#overlay').classList.add('open')}function closeCart(){$('#cart').classList.remove('open');$('#overlay').classList.remove('open')}$('#openCart').onclick=openCart;$('#closeCart').onclick=closeCart;$('#overlay').onclick=closeCart;$('#checkout').onclick=()=>{if(!cart.length){alert('Your bag is empty.');return}$('#modal').classList.add('open')};$('#closeModal').onclick=()=>$('#modal').classList.remove('open');$('#cancelOrder').onclick=()=>$('#cancelModal').classList.add('open');$('#closeCancelModal').onclick=()=>$('#cancelModal').classList.remove('open');
$('#orderForm').onsubmit=e=>{e.preventDefault();const data=new FormData(e.target);const orderId='HVX-'+Date.now().toString().slice(-7);const subtotal=cart.reduce((n,x)=>n+PRODUCTS.find(p=>p.id===x.id).price*x.qty,0);const total=subtotal+STORE.delivery;const lines=cart.map(x=>{const p=PRODUCTS.find(p=>p.id===x.id);return `• ${p.name} — Size ${x.size} × ${x.qty} — ${money(p.price*x.qty)}`}).join('\n');const message=`HAVOX ORDER\nOrder No: ${orderId}\n\n${lines}\n\nSubtotal: ${money(subtotal)}\nDelivery: ${money(STORE.delivery)}\nTOTAL: ${money(total)}\n\nCustomer: ${data.get('name')}\nPhone: ${data.get('phone')}\nAddress: ${data.get('address')}\nPayment: ${data.get('payment')}\n\nDelivery: Islandwide Sri Lanka, 2–5 working days.`;window.open(`https://wa.me/94751804730?text=${encodeURIComponent(message)}`,'_blank','noopener,noreferrer');localStorage.setItem('havoxLastOrder',JSON.stringify({id:orderId,phone:data.get('phone'),created:Date.now()}));cart=[];save();$('#modal').classList.remove('open');closeCart();e.target.reset()};
$('#cancelForm').onsubmit=e=>{e.preventDefault();const data=new FormData(e.target);const message=`HAVOX CANCELLATION REQUEST\nOrder No: ${data.get('order')}\nPhone: ${data.get('phone')}\nReason: ${data.get('reason')||'Not provided'}\n\nPlease confirm whether this order can be cancelled.`;window.open(`https://wa.me/94751804730?text=${encodeURIComponent(message)}`,'_blank','noopener,noreferrer');$('#cancelModal').classList.remove('open');e.target.reset()};
async function init(){try{const res=await fetch('HAVOX_PRODUCT_DATA.md',{cache:'no-store'});const text=await res.text();PRODUCTS=parseData(text)}catch(e){PRODUCTS=[{id:1,name:'straw hat will',color:'black',gsm:190,price:2100,stock:{M:2,L:4,XL:2},image:'1. Straw hat will.png'},{id:2,name:'cursed king',color:'black',gsm:190,price:2100,stock:{M:1,L:2,XL:1},image:'2.Cursed king.png'},{id:3,name:'sun breather',color:'white',gsm:220,price:2300,stock:{M:2,L:4,XL:2},image:'3.Sun breather.png'},{id:4,name:'Destiny forged',color:'white',gsm:220,price:2300,stock:{M:1,L:2,XL:1},image:'4. Destiny forged.png'}]}renderProducts();renderSizeGuide();renderSocial();renderCart()}init();